import { 
    // createBrowserHistory, 
    createHashHistory 
} from 'history';
// import config from '_config';

// export const history = createBrowserHistory({ basename: config.app.basename });
export const history = createHashHistory();
